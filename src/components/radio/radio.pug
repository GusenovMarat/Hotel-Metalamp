mixin radio({items, title})
	.radio 
		if title 
			.radio__title=title
		.radio__wrap	
			each item in items
				- const { type, name, text } = item
					label.radio__label  
						input.radio__real(
							type=type,
							name=name
						)
						span.radio__custom
						span.radio__text=text
