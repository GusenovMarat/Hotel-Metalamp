mixin footer-nav({title, list, hidden=false})
	.footer-nav(class= hidden ? "footer-nav__hidden-lg" : "")
		span.footer-nav__title=title
		ul.footer-nav__list
			each item in list
				-	const { href, text } = item
				li.footer-nav__item
					a.footer-nav__link(href=href || "./index.html")=text

