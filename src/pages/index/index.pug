extends ../../layouts/main/main
include ../../components/index/index

block variables
	- var title = ""
	- var disctiption = "Colors and fonts"
	- var logo = "logo-grey.svg"
	- var background = false;

block content
	- const {colors} = require(./colors_and_types.json)

	.content
		.content_colors
			each element in colors
				+colors(element)
		.content_types
			+types