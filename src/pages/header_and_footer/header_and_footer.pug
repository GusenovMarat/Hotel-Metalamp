extends ../../layouts/main/main
include ../../components/header/header
include ../../components/footer/footer

block variables
	- var title = "H&F";
	- var discription = "Header and Footer";
	- var logo = "logo-grey.svg";
	- var background = true;

block content
	- const { header, headerAccount, footer } = require("./header_and_footer.json")

	.header-footer 
		+header(header)
		+header(headerAccount)
		+footer(footer)